# vts-v1 Human Test Plan

Coverage validation: **PASS**. 32 automated criteria covered (AC1.3 is manual-only by design).

`source venv/bin/activate && python -m pytest -q` should return "77 passed, 1 deselected" (the 1 deselected is `TestCloudIntegration::test_end_to_end_real_video`, opted-in via `RUN_CLOUD_TESTS=1`).

## Prerequisites

- Worktree at `/home/tfish/dev_claude/video-to-steps/.worktrees/vts-v1`
- Venv activated: `source venv/bin/activate`
- `.env` populated with:
  - `LLM_API_KEY`, `VISION_API_KEY`, `JINA_API_KEY`
  - `LLM_BASE_URL`, `LLM_MODEL`, `VISION_BASE_URL`, `VISION_MODEL`
  - `EMBED_BACKEND=jina_v4`, `APP_HOST=127.0.0.1`, `APP_PORT=8090`
- ffmpeg installed on PATH
- Working internet (yt-dlp + cloud providers)
- A short instructional YouTube URL (the cloud test uses `https://www.youtube.com/watch?v=8VVXqQv2WUk` — NetKnots Bowline knot tutorial, ~2 min)

## Phase 1: Cloud-path end-to-end (AC1.1, AC1.2, AC1.3, AC2.1, AC7.1)

The single cloud-marked automated test, run manually because it costs real money.

| Step | Action | Expected |
|------|--------|----------|
| 1 | `cd ~/dev_claude/video-to-steps/.worktrees/vts-v1 && source venv/bin/activate` | Shell prompt shows `(venv)` |
| 2 | `RUN_CLOUD_TESTS=1 python -m pytest -m cloud tests/pipeline/test_pipeline.py::TestCloudIntegration::test_end_to_end_real_video -s` | Test passes. Stdout prints `Step N: <instruction>` lines and frame paths |
| 3 | Read each printed `Step N: <instruction>` line | Each is 1–3 sentences, starts with imperative verb ("Form", "Pass", "Pull", "Tighten"). NOT third person or descriptive |
| 4 | Open each printed frame path alongside its step | ≥80% of steps have a thumbnail visibly depicting the action/tool/material named. No frame contradicts its instruction |
| 5 | Locate temp `jobs_root` printed in pytest output; inspect `<tmp>/<job_id>/` | Contains `meta.json`, `steps.json`, `outline.json`, `frame_captions.json`, `video.mp4`, `*.en.vtt`, `frames/NNNN.jpg`. `meta.json.status == "done"`, `cost.total_usd > 0`, `len(steps.json) >= 3` |

## Phase 2: Browser smoke (AC8 end-to-end)

| Step | Action | Expected |
|------|--------|----------|
| 1 | `./start.sh` | Uvicorn starts on `127.0.0.1:8090`; `.vts.pid` exists in repo root |
| 2 | Open `http://127.0.0.1:8090/` in browser | 200; form has URL input + Submit; no console errors |
| 3 | Paste instructional URL, click Submit | Address bar → `/job/<12-hex-chars>`; status page shows `queued` or `running` |
| 4 | Watch status page | Progress advances: downloading → parsing captions → extracting frames → embedding → outlining → matching → captioning → refining. Running cost grows. DevTools Network shows XHR to `/job/<id>/status` every 2s |
| 5 | When status is `Done`, check Network tab | Polling stops; "View result" link visible |
| 6 | Click "View result" | `/job/<id>/result`. ≥3 ordered steps; each has 1–3 sentence imperative instruction + ≥1 thumbnail. Every `<img>` has non-empty `alt`. Small print shows mode, total_usd, jina_v4, vision model, LLM model |
| 7 | Click any thumbnail | Opens `/job/<id>/frame/NNNN.jpg` |
| 8 | `./stop.sh` | Server shuts down cleanly; `.vts.pid` removed |

## Phase 3: Targeted manual verifications

### M1 — frames visibly relevant to instructions (AC1.3)
Covered by Phase 1 step 4 and Phase 2 step 6. Pass criterion: ≥80% of steps have at least one visibly relevant frame; no frame contradicts its instruction.

### M2 — vision caption focuses on actions/tools/materials (AC4.5)

| Step | Action | Expected |
|------|--------|----------|
| 1 | `python -m scripts.smoke_vision` | Prints a caption for `tests/providers/fixtures/test_frame.jpg` + token usage |
| 2 | Read printed caption | 1–2 complete sentences. Mentions at least one of: action verb, named tool, named material. Generic "a kitchen with good lighting" is a FAIL |

### M3 — README deployment warning is substantive (AC9.2)

| Step | Action | Expected |
|------|--------|----------|
| 1 | Open `README.md` in a markdown renderer | Locate the section that mentions `APP_HOST=0.0.0.0` |
| 2 | Read the section | Explicitly states 0.0.0.0 exposes the server, that a reverse proxy (or VPN/Tailscale) is required, and that no auth is built into vts-v1. Warning is prominent, not parenthetical. A new reader skimming this section understands they must put something in front of vts-v1 before binding 0.0.0.0 |

### M4 — instruction wording is imperative second-person (AC1.2 supplement)

| Step | Action | Expected |
|------|--------|----------|
| 1 | Read every step's `instruction` text from Phase 1 stdout or Phase 2 step 6 | Each starts with imperative verb. NOT "The chef adds…". NOT "Eggs are added". 1–3 sentences. 100% compliance — any failure is a hard fail |

## Phase 4: Reinforcement checks

### Atomic write under load (AC7.2 reinforcement)

1. Start server (`./start.sh`) and submit a real URL.
2. While job is `running`, in another terminal:
   `for i in $(seq 1 100); do curl -s http://127.0.0.1:8090/job/<id>/status | head -c 1 > /dev/null && echo ok || echo FAIL; done`
3. Pass: 100/100 print `ok`, 0 print `FAIL`.

### Mode A error message readability (AC4.4 supplement)

1. Edit `.env`: set `EMBED_BACKEND=mlx_clip`.
2. `python -c "from config import get_settings; from providers.embed import build_embedder; build_embedder(get_settings())"`.
3. Expected: `RuntimeError` whose message contains "mlx_clip not installed" + pip-install hint or Mac-only note. Confirm the message is actually readable (the automated test asserts the regex; humans confirm wording quality).
4. Restore `EMBED_BACKEND=jina_v4`.

## Human Verification Required

| Criterion | Why Manual | Step Reference |
|-----------|------------|----------------|
| AC1.1 | Cloud API costs money | Phase 1 step 2 |
| AC1.2 | Imperative-mood judgment beyond regex | Phase 1 step 3 + M4 |
| AC1.3 | Visual-relevance judgment | Phase 1 step 4 + M1 |
| AC2.1 | Live yt-dlp + ≤720p verification | Phase 1 step 5 or `python -m scripts.smoke_phase3 <url>` |
| AC4.5 | Caption content quality | M2 |
| AC7.1 | Verify positive `total_usd` against real pricing | Phase 1 step 5 |
| AC9.2 | Warning prose substance | M3 |
| AC9.3 | Mode A/B docs accuracy | Skim README Mode A/B; confirm env names match `Field(alias=...)` lines in `config.py` |

## Traceability

| Acceptance Criterion | Automated Test | Manual Step |
|----------------------|----------------|-------------|
| AC1.1 | `test_pipeline.py::TestCloudIntegration::test_end_to_end_real_video` (cloud) + `TestUnknownModelPricingZero` (offline stubs) | Phase 1 step 2 |
| AC1.2 | `test_llm_refine.py::test_refine_produces_imperative_sentences` | M4 |
| AC1.3 | (none) | M1 / Phase 1 step 4 |
| AC2.1 | (yt-dlp mocked in unit suite) | `scripts/smoke_phase3.py`; Phase 1 step 5 |
| AC2.2 | `test_captions.py::test_parse_vtt` | — |
| AC2.3 | `test_captions.py::test_dedupe_rolling_*` | — |
| AC2.4 | `test_pipeline.py::TestCaptionlessVideoErrorPath::test_no_captions_sets_error_status` | — |
| AC3.1 | `test_frames.py::test_fixed_fps_extracts_one_per_second_at_720_or_less` | — |
| AC3.2 | `test_frames.py::test_dedup_strictly_reduces_on_cut_heavy_video` + `test_dedup_equal_or_fewer_on_any_input` | — |
| AC3.3 | `test_frames.py::test_phash_filter_never_drops_first` | — |
| AC4.1 | `test_llm_client.py::test_llm_client_openai_shape` + `test_llm_client_qwen_shape` | — |
| AC4.2 | `test_llm_client.py::test_llm_client_strips_think_tags` | — |
| AC4.3 | `test_jina_embedder.py::test_jina_embedder_embed_images` + `test_jina_embedder_embed_texts` + `test_jina_embedder_consistent_dim` | — |
| AC4.4 | `test_embed_factory.py::test_embed_factory_*` + `test_mlx_clip_factory.py` | Mode A error message reinforcement |
| AC4.5 | `test_vision_captioner.py::test_vision_captioner_returns_caption` + `test_vision_captioner_reads_system_prompt` | M2 |
| AC5.1 | `test_llm_outline.py::test_outline_well_formed_json_object_with_steps_key` | — |
| AC5.2 | `test_llm_outline.py::test_outline_well_formed_json_object_with_steps_key` | — |
| AC5.3 | `test_llm_outline.py::test_outline_slice_fallback_with_prose` + `test_slice_finds_balanced_brackets_inside_strings` + `test_parse_outline_raises_on_unrecoverable` | — |
| AC5.4 | `test_llm_refine.py::test_refine_produces_imperative_sentences` + `test_refine_parses_object_with_prose_around` + `test_refine_falls_back_when_chat_fails` | — |
| AC6.1 | `test_match.py::TestMatchWindowRestriction::test_window_restriction_with_padding` | — |
| AC6.2 | `test_match.py::TestMatchTopK::test_top_k_ranking_by_cosine` | — |
| AC6.3 | `test_match.py::TestMatchEmptyWindowFallback::test_fallback_to_nearest_midpoint` | — |
| AC7.1 | `test_pipeline.py::TestUnknownModelPricingZero` (sum-equality) + cloud test | Phase 1 step 5 |
| AC7.2 | `test_pipeline.py::TestAtomicWriteContract::test_atomic_write_never_tears` + `test_server.py::test_status_fragment_running` | Atomic write under load |
| AC7.3 | `test_pipeline.py::TestUnknownModelPricingZero::test_unknown_model_costs_zero_and_warns` | — |
| AC8.1 | `test_server.py::test_index_renders_form` | Phase 2 step 2 |
| AC8.2 | `test_server.py::test_process_redirects_303_and_writes_manifest` + `test_process_accepts_known_youtube_shapes` | Phase 2 step 3 |
| AC8.3 | `test_server.py::test_process_rejects_non_youtube_400` | — |
| AC8.4 | `test_server.py::test_status_fragment_running` + `test_status_fragment_done_stops_polling` + `test_status_fragment_error_stops_polling` | Phase 2 steps 4-5 |
| AC8.5 | `test_server.py::test_result_page_renders_steps_and_meta` + `test_result_redirects_when_not_done` + `test_unknown_job_id_404` | Phase 2 steps 6-7 |
| AC9.1 | structural: `config.py` default `"127.0.0.1"`; `.env.example` `APP_HOST=127.0.0.1` | — |
| AC9.2 | structural: README grep `APP_HOST=0.0.0.0` + `reverse proxy` | M3 |
| AC9.3 | structural: README `Mode A` × 3, `Mode B` × 2, `UNTESTED` × 2 | Mode A/B docs accuracy in Human Verification table |
